public interface ClubAdminTools extends FinanceTool, MinutesTool, EventsTool {
    // Composite interface — kept for backward compatibility.
    // Each role should depend on the smallest interface it needs.
}
